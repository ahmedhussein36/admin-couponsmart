import React from "react";
import TagClient from "./TagClient";

const TagsPage = () => {
    return (
        <>
            <title>Posts : tags</title>
            <TagClient />
        </>
    );
};

export default TagsPage;
